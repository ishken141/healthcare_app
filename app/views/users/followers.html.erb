<% @users.each do |user| %> 
<%= image_tag user.profile.image.url, class: "follow_image" %>
<%= link_to "#{user.name}", user_path(user), method: :get %> 
<%= link_to "#{user.profile.certificaton}"%>
<% end %>