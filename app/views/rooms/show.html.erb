
<% @user.messages.each do |message| %> 
<%= message.text %> 
<%= message.user.name %>
<%= link_to "削除", user_message_path(@user, message), method: :delete %>
<% end %>
<h1>会話する場所</h1>
<%= form_with model: [@user, @message], local: true do |f| %>
<%= f.text_field :text %> 
<%= f.submit %>
<% end %>