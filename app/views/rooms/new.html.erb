<div class="new_room">
<%= form_with model: @room, local: true do |f| %> 
<div class="form-group"> 
<label>相談室名</label> 
<%= f.text_field :box, class: "room_group"%> 
<%= f.submit "開設"%>
<% end %>
</div>